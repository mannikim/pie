pie: mannikim's personal image editor
===
provides `pie`, the image editor, `pcp`, a color picker program and `piec`, a
program to interact with a pie instance via unix-domain sockets

features
- uses farbfeld as the image format. see https://tools.suckless.org/farbfeld
- stdin/stdout
- area selection
- simple terminal ui
- unix-domain socket interface
- <2k loc

usage
---
use `pie -h` for usage

pie uses stderr for displaying a simple tui. this allows the program to
display text-based information without implementing font loading & rendering
to the program

pcp takes no arguments and always returns the selected color in the stdout

piec requires the socket path as the first argument and the command as the
second argument

configuring
---
configure pie by editing the source code and recompiling

build
---
building requires a c99 compiler, libm, GLFW and GLEW

building

    make

install

    make install

+++ todo +++
- versioning
- proper api for sockets
# pie
# pcp
- alpha slider
+++ end todo +++
